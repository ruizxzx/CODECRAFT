# OFFSCRPT V83.2.0 — Carousel Fit + Responsive Crop Preview

## Implemented
- Removed the outer black framing/padding around the home carousel so slides occupy the available width without letterbox-like black bars.
- Carousel uses a responsive render frame: 16:9 on mobile and 21:9 on larger screens.
- Preserved `object-fit: cover` so image slides always fill the frame.
- Added admin responsive crop preview for both Desktop (21:9) and Mobile (16:9).
- Crop preview uses the saved X/Y position and zoom values in real time.
- Clicking the crop preview updates crop position.
- Added visible crop-state/safe-area guidance.
- Preserved all existing carousel Firestore fields and ordering/edit/delete behavior.
- Removed a duplicate `getCarouselSlides()` declaration present in the V83.1 source package.

## Compatibility
- No Firestore schema or rules changes.
- Existing carousel documents remain compatible.
- Existing routes/authentication/AI/social functionality preserved.

## Validation
- V83 mobile checker: pass.
- V83.1 hardening checker: pass.
- Firebase/rules/import checks: pass.
- Full dependency-backed TypeScript/Vite build: blocked because the package is shipped without node_modules and the environment cannot currently restore all dependencies.
