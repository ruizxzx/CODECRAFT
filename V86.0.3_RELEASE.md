# OFFSCRPT V86.0.3

## Commerce Admin Authentication Fix
- Fixed Master Control → Commerce Diagnostics returning “Administrator access required” for bootstrap master-admin accounts.
- Backend authentication now retrieves the authenticated account email/verification state and mirrors the canonical OFFSCRPT bootstrap admin allowlist.
- Configured `masterAdmins/{uid}` and `masterAdminEmails/{email}` access remains supported.
- Unauthorized diagnostics requests return HTTP 403.

## Validation
Static rules/commerce checks pass. Full dependency-backed typecheck/build must be run in the deployment environment where dependencies are installed.
