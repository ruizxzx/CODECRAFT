OFFSCRPT V81.1.0 — NAVIGATION / BRANDING / AI-KNOWLEDGE UNIFICATION
Validation report

IMPLEMENTED
- Primary desktop navigation is normalized to Home / SCRPTS / Community / Explore.
- BLOG user-facing terminology is changed to SCRPTS across publishing/community/discovery UI surfaces while legacy blog route/schema identifiers remain intact for compatibility.
- Knowledge and the primary Ask AI entry are unified under ASK OFFSCRPT.
- Existing Site-wide AI Copilot remains available as a secondary utility through the menu and Command Palette; it is not removed.
- Existing /#blog route remains supported.
- Existing V80/V81 retrieval, permissions, Firestore and AI gateway architecture is preserved.
- Command Palette uses SCRPTS / Ask OFFSCRPT terminology.
- Mobile AI entry routes to the unified Ask OFFSCRPT workspace.

PASSED
- check:deployment-config
- check:imports — 114 active source files
- check:exports — 114 active source files
- check:identifiers — 114 active source files
- check:firebase
- check:rules
- check:schema
- check:runtime
- check:recommendations
- check:handles
- check:identity-sync
- check:public-profile
- check:comments
- check:site-sync
- check:v80
- check:v81
- check:v81-navigation
- TypeScript parser syntax validation — 114 TS/TSX files, 0 parse diagnostics

NOT RUN / ENVIRONMENT BLOCKED
- npm ci / dependency installation: registry/network operation timed out.
- Full TypeScript project typecheck: project dependencies are not installed; global TypeScript can parse files but required @types and runtime packages are absent.
- Vite production build: Vite and application dependencies are not installed, so a real production build cannot be honestly claimed.

IMPORTANT
The release is not described as build-verified or bug-free because dependency-backed typecheck/build could not complete in this environment.

CLEANUP
- node_modules and temporary validation-only dependency copy are removed before packaging.
