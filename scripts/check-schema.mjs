import fs from 'node:fs'; const blueprint=JSON.parse(fs.readFileSync('firebase-blueprint.json','utf8')); const cms=fs.readFileSync('src/lib/cms.ts','utf8'); const entities=blueprint.entities||{}; const required=['User','Post','Comment']; const missing=required.filter(x=>!entities[x]); if(missing.length){console.error('SCHEMA CHECK FAILED:',missing.join(', '));process.exit(1)} if(!cms.includes('function normalizeArticleRecord')){console.error('SCHEMA CHECK FAILED: article normalization missing.');process.exit(1)} console.log(`SCHEMA CHECK OK — ${Object.keys(entities).length} blueprint entities validated.`);
