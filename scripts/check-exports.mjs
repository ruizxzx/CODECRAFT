import fs from 'node:fs'; import path from 'node:path'; import ts from 'typescript';
const root=path.resolve('src'); const files=[];
function walk(p){const st=fs.statSync(p);if(st.isFile()){if(/\.(ts|tsx)$/.test(p))files.push(p);return;}for(const e of fs.readdirSync(p,{withFileTypes:true})){if(['node_modules','dist'].includes(e.name))continue;walk(path.join(p,e.name));}}
walk(root);const byPath=new Map(files.map(f=>[path.resolve(f),f]));
function resolve(spec,from){const base=path.resolve(path.dirname(from),spec);for(const ext of ['.ts','.tsx','.js','.jsx'])if(byPath.has(path.resolve(base+ext)))return path.resolve(base+ext);for(const ext of ['.ts','.tsx'])if(byPath.has(path.resolve(base,'index'+ext)))return path.resolve(base,'index'+ext);return null;}
function exportsOf(file){const sf=ts.createSourceFile(file,fs.readFileSync(file,'utf8'),ts.ScriptTarget.ES2022,true,file.endsWith('.tsx')?ts.ScriptKind.TSX:ts.ScriptKind.TS);const out=new Set();function visit(n){if(n.modifiers?.some(m=>m.kind===ts.SyntaxKind.ExportKeyword)){if(n.name?.text)out.add(n.name.text);if(ts.isVariableStatement(n))for(const d of n.declarationList.declarations)if(ts.isIdentifier(d.name))out.add(d.name.text);}ts.forEachChild(n,visit)}visit(sf);return out}
const cache=new Map(),failures=[];for(const file of files){const sf=ts.createSourceFile(file,fs.readFileSync(file,'utf8'),ts.ScriptTarget.ES2022,true,file.endsWith('.tsx')?ts.ScriptKind.TSX:ts.ScriptKind.TS);for(const n of sf.statements){if(!ts.isImportDeclaration(n)||!ts.isStringLiteral(n.moduleSpecifier))continue;const spec=n.moduleSpecifier.text;if(!spec.startsWith('.'))continue;const target=resolve(spec,file);if(!target)continue;if(!cache.has(target))cache.set(target,exportsOf(target));const ex=cache.get(target);const c=n.importClause;if(!c)continue;if(c.name&&!ex.has('default'))failures.push(`${path.relative(root,file)} imports default from ${spec}, but target has no default export`);if(c.namedBindings&&ts.isNamedImports(c.namedBindings))for(const el of c.namedBindings.elements){const imported=(el.propertyName||el.name).text;if(!ex.has(imported))failures.push(`${path.relative(root,file)} imports ${imported} from ${spec}, but target does not export it`)}}}
if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log(`EXPORT CHECK OK — ${files.length} active source files scanned.`);
