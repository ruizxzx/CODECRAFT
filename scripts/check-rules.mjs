import fs from 'node:fs'; const rules=fs.readFileSync('firestore.rules','utf8'); const required=["rules_version = '2';",'service cloud.firestore','function isSignedIn()','function isAdmin()','function isPlatformModerator()','function hasModeratorPermission(permission)','match /{document=**}']; const missing=required.filter(x=>!rules.includes(x)); if(missing.length){console.error('RULES CHECK FAILED:',missing.join(', '));process.exit(1)} const firstMatch=rules.indexOf('match /{document=**}'); if(!rules.slice(firstMatch,firstMatch+200).includes('allow read, write: if false')){console.error('RULES CHECK FAILED: default deny is missing.');process.exit(1)} const opens=(rules.match(/{/g)||[]).length, closes=(rules.match(/}/g)||[]).length; if(opens!==closes){console.error(`RULES CHECK FAILED: brace mismatch ${opens} vs ${closes}`);process.exit(1)} console.log(`RULES CHECK OK — balanced v2 rules (${opens} braces), granular staff helpers, and default deny present.`);