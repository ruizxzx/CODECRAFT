# OFFSCRPT V88.0.5 — Product Page React Hook Crash Fix

## Fix
- Fixed `CommerceProductView` React error #310 by ensuring the product gallery `useMemo` hook executes unconditionally before loading/error early returns.
- Prevents hook-order changes during the product loading → loaded transition.
- Preserves the V88.0.4 dedicated product R2 credentials and upload architecture.

## Validation
- Source inspection confirms all hooks in `CommerceProductView` execute before any conditional return.
- TypeScript syntax validation performed with the globally installed TypeScript compiler.
- Existing deployment, commerce, storefront, Firebase, Firestore-rules, schema, runtime, and V88 checks retained unchanged.

## User-visible result
Creator storefront product links can transition from loading state to the product page without triggering React minified error #310.
