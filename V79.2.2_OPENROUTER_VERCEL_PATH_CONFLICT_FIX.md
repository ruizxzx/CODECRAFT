# V79.2.2 — OpenRouter Vercel Path Conflict Fix

- Renamed the server-side provider from `api/ai/openrouter.js` to `api/ai/openrouter-provider.js`.
- Updated the AI gateway and discussion-summary imports to the unique provider path.
- This avoids Vercel function-name collisions with a legacy `api/ai/openrouter.ts` that may still exist in the Git history/repository.
- Kept OpenRouter, Firebase auth, retry/fallback routing, structured-output fallback, caching, and existing V79 AI functionality unchanged.
