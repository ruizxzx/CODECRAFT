# V79.1.0 — AI Gateway Runtime Fix

- Fixed Gemini 500 failures by using direct Gemini REST requests with `x-goog-api-key`, compatible with current AI Studio auth-key handling.
- Added model discovery and fallback across currently available Flash models so stale model environment variables do not break AI features.
- Added explicit AI authentication, quota, model, and structured-response errors.
- Preserved Firebase ID-token verification and all V79 AI features.
- Fixed duplicate identical AI error toasts stacking repeatedly.
- Fixed authenticated article-view tracking so it no longer attempts unauthorized reads from the analytics receipt collection.
