# V77.0.5 — Question Runtime + Theme Hardening

## Fixed

### React error #310 on question pages
`QuestionView` had a `useMemo` for `childrenByParent` after loading/not-found/removed early returns. This caused different hook counts between renders. The memo is now initialized before every conditional return.

### Cleanup
Removed an empty effect that had no observable behavior.

## Retained

- V77 Q&A engine
- Existing answer voting, replies, accepted answers, follows and moderation
- Existing R2 uploads and image crop/preview
- V76 social feed/discovery
- Firestore quota optimization
- V77.0.4 guest-first-visit light theme behavior

## Verification

- TypeScript/TSX parser validation
- Duplicate exported-symbol check
- QuestionView hook ordering review
