# V77.0.4 — Question Answer Fix

- Fixes normal authenticated users being blocked when creating answers because the answer batch also updates question/reply counters.
- Uses atomic `getAfter()` rule validation so counters remain protected from arbitrary writes.
- Makes answer media previews compact, responsive, and non-destructive with remove controls.
- Keeps R2 uploads, crop/preview, Firebase Auth, Firestore, feed, recommendations, and existing Q&A functionality intact.
