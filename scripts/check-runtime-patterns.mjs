import fs from 'node:fs';import path from 'node:path';const root=path.resolve('src');const files=[];function walk(p){const st=fs.statSync(p);if(st.isFile()){if(/\.(ts|tsx)$/.test(p))files.push(p);return;}for(const e of fs.readdirSync(p,{withFileTypes:true})){if(['node_modules','dist'].includes(e.name))continue;walk(path.join(p,e.name));}}walk(root);const findings=[];for(const file of files){const text=fs.readFileSync(file,'utf8');if(/catch\s*\{\s*\}/.test(text))findings.push(`${path.relative(root,file)}: empty catch block`);if(/setInterval\s*\([^,]+,\s*[0-9]{1,3}\s*\)/.test(text)&&!file.endsWith('HomeView.tsx'))findings.push(`${path.relative(root,file)}: suspicious high-frequency interval`)}if(findings.length){console.error(findings.join('\n'));process.exit(1)}console.log(`RUNTIME PATTERN CHECK OK — ${files.length} active source files scanned.`);
